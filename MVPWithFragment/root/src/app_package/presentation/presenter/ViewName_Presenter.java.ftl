package ${packageName}.presentation.presenter;

import android.support.annotation.NonNull;

import ${packageName}.presentation.view.fragment.${viewName}ViewFragment;

/**
 * RecipeList Presenter layer.
 */
public class ${viewName}Presenter extends BasePresenter<${viewName}ViewFragment> {

    public ${viewName}Presenter(@NonNull ${viewName}ViewFragment view) {
        super(view);
    }

}